// @flow

import React from 'react';

const blackStyle = {
  background: '#000',
  width: '40px',
  height: '40px',
  margin: '5px',
  display: 'inline-block',
  borderRadius: '100%'
};

const Black = () => <div style={blackStyle} />;

export default Black;
