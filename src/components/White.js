
import React from 'react';

const whiteStyle = {
  background: '#fff',
  width: '40px',
  height: '40px',
  margin: '5px',
  display: 'inline-block',
  borderRadius: '100%'
};

const White = () => <div style={whiteStyle} />;

export default White;
